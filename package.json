{
  "name": "learning-management-system",
  "version": "1.0.0",
  "description": "Comprehensive Learning Management System with E2E testing",
  "private": true,
  "scripts": {
    "e2e": "npm run docker:test && sleep 5 && E2E_BASE_URL=http://localhost:3002 playwright test --project=chromium --reporter=line",
    "e2e:all": "npm run docker:test && sleep 5 && E2E_BASE_URL=http://localhost:3002 playwright test",
    "e2e:ui": "npm run docker:test && sleep 5 && E2E_BASE_URL=http://localhost:3002 playwright test --ui",
    "e2e:headed": "npm run docker:test && sleep 5 && E2E_BASE_URL=http://localhost:3002 playwright test --headed",
    "e2e:debug": "npm run docker:test && sleep 5 && E2E_BASE_URL=http://localhost:3002 playwright test --debug",
    "e2e:affected": "node scripts/run-affected-e2e-tests.js",
    "e2e:install": "npx playwright install chromium",
    "e2e:report": "npx playwright show-report",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "docker:up": "node scripts/check-ports.js development && docker compose up -d",
    "docker:down": "docker compose down",
    "docker:test": "node scripts/check-ports.js test && docker compose -f docker-compose.test.yml up -d",
    "docker:test:down": "docker compose -f docker-compose.test.yml down",
    "docker:cleanup": "node scripts/docker-cleanup.js",
    "check-ports": "node scripts/check-ports.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^24.2.0",
    "typescript": "^5.9.2",
    "eslint": "^9.30.1",
    "@typescript-eslint/eslint-plugin": "^8.38.0",
    "@typescript-eslint/parser": "^8.38.0",
    "prettier": "^3.6.2",
    "eslint-config-prettier": "^10.1.8",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "vite": "^7.0.4"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
